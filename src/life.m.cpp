// #include "game_engine.hpp"

#include <fmt/format.h>
#include <spdlog/spdlog.h>
#include <optional>
#include <string>
#include <unordered_map>
#include <variant>

auto main(int argc, char** argv) -> int
{
    // life::GameSettings settings(argc, argv);
    // life::GameEngine<BasicRules> engine;

    return 0;
}